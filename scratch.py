import numpy as np
import pickle as pk

# d = {}
# a = np.arange(10).reshape(2,5)
# b = np.arange(100).reshape(20,5).astype("float32")
# d["a"] = a
# d["b"] = b
# with open('data.pkl', 'wb') as output:
#     pk.dump(d, output)
# with open("data.pkl", "rb") as input:
# 	l = pk.load(input)
# print(l.keys())

print(2/3)
